{
  "_comment": "One-off event additions and corrections, merged into every weekly run. For PERMANENT venues (museums, zoos, domains, playgrounds, zomerbars, playground-restaurants) use data/places.json instead — those are not re-fetched. Any classification field set here bypasses the Claude step.",
  "activities": []
}

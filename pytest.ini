[pytest]
# automation/ modules import each other by bare name (import config, import
# sources), so the package dir itself has to be on sys.path.
pythonpath = automation
testpaths = automation/tests
addopts = -q --strict-markers
filterwarnings =
    error::DeprecationWarning:automation.*
